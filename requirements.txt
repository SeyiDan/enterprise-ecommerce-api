# Pinned working set. Verified 2026-08-25: `pytest --cov=app` -> 42 passed, 97% coverage
# in a clean venv built from this file.
#
# bcrypt is pinned to 4.x deliberately. passlib 1.7.4 reads bcrypt's removed
# __about__.__version__ and mis-handles the 72-byte password limit against
# bcrypt 5.x, which fails 19 of 21 tests with
# "ValueError: password cannot be longer than 72 bytes" on any clean install.
# Do not unpin without replacing passlib.

# Web framework
fastapi==0.141.1
starlette==1.3.1
python-multipart==0.0.32

# ASGI server. This is what the Dockerfile CMD and docker-compose invoke, and it
# is required to serve the application. Note that the test suite does not
# exercise it: pytest drives the app through httpx's ASGI transport in-process,
# so a missing server is not something the tests can detect.
uvicorn[standard]==0.52.4

# Data validation
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0

# Database
SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
alembic==1.13.2

# Auth
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.5.0

# Config
python-dotenv==1.2.2

# Test
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1
